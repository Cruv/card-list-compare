# Required in production — used to sign JWT auth tokens
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=

# Path to SQLite database file (default: ./server/data/cardlistcompare.db in dev, /app/data/cardlistcompare.db in Docker)
# DB_PATH=./server/data/cardlistcompare.db

# Express server port (default: 3001, nginx handles port 80 in Docker)
# PORT=3001
