# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
server/data/*.db*
server/data/.jwt-dev-secret
server/data/image-cache/
server/data/downloads/
data/.jwt-dev-secret
data/*.db*
data/image-cache/
data/downloads/
dist-ssr
*.local

# Secrets / local env (JWT_SECRET, SMTP_PASS, …) — never commit
.env
.env.*
!.env.example

# Security audit docs — kept local; this repo is public and the audits contain
# exploit detail. Move to a private repo if you want them tracked (WarSlate-style).
docs/audit-*.md

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.claude/settings.local.json
CLAUDE.local.md
CLAUDE.local.archive.md
nul
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
